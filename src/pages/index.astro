---
import Layout from "../layouts/Layout.astro";
import Nav from "../components/Nav.astro";
import Hero from "../components/Hero.astro";
import Ecosystem from "../components/Ecosystem.astro";
import Marketplace from "../components/Marketplace.astro";
import AISuite from "../components/AISuite.astro";
import Lab from "../components/Lab.astro";
import Stats from "../components/Stats.astro";
import Personas from "../components/Personas.astro";
import SiteFooter from "../components/SiteFooter.astro";
---

<Layout title="Threadzip — The Fabric Industry Ecosystem" description="AI-Powered Fabric Marketplace, Analysis, and Generation.">
  <Nav />
  <Hero />
  <div class="divider"></div>
  <Ecosystem />
  <div class="divider"></div>
  <Marketplace />
  <div class="divider"></div>
  <AISuite />
  <div class="divider"></div>
  <Lab />
  <div class="divider"></div>
  <Stats />
  <div class="divider"></div>
  <Personas />
  <div class="divider"></div>
  <SiteFooter />
</Layout>

<script>
  // Scroll reveal
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        (e.target as HTMLElement).style.opacity = "1";
        (e.target as HTMLElement).style.transform = "translateY(0)";
        observer.unobserve(e.target);
      }
    });
  }, { threshold: 0.1 });

  document.querySelectorAll(".eco-card, .persona-card, .feature-split, .stat-item").forEach(el => {
    (el as HTMLElement).style.opacity = "0";
    (el as HTMLElement).style.transform = "translateY(20px)";
    (el as HTMLElement).style.transition = "opacity 0.6s cubic-bezier(0.22,1,0.36,1), transform 0.6s cubic-bezier(0.22,1,0.36,1)";
    observer.observe(el);
  });
</script>
